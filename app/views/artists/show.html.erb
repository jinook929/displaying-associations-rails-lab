<h1><%= @artist.name %></h1>

<h3><%= @artist.name %>'s Songs</h3>
<p><%= pluralize(@artist.songs.count, 'song') %></p>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song %></li>
  <% end %>
</ul>